import StaticPage from '@/shared/ui/staticPage/StaticPage';

export default function CareersPage() {
  return (
    <StaticPage title="Вакансии">
      <p>Прямо сейчас открытых позиций нет, но команда растёт — загляните позже.</p>
    </StaticPage>
  );
}
